https://www.getgymbo.com/* https://getgymbo.com/:splat 301

# Instagram bio short-link → UTM-tagged homepage (so Umami attributes IG traffic)
/ig https://getgymbo.com/?utm_source=instagram&utm_medium=organic_social&utm_campaign=bio 301

# Referral short-link /r/<token> is NOT here: it is functions/r/[token].js (gy-3jb0t).
# A rule here sent the LITERAL ":token" as utm_campaign, because Pages does not
# substitute placeholders in the query string.
