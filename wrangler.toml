name = "gymbo-landing"
pages_build_output_dir = "./dist"
compatibility_date = "2026-02-27"

# Waitlist backend: now uses Supabase REST (functions/api/waitlist.js), NOT D1.
# No binding needed — the function inlines the PUBLIC Supabase anon key. The old
# D1 plan (GYM-597) was dropped because D1 provisioning needs a CF D1:Edit token
# the Pages-deploy token lacks. See SETUP-BACKEND.md.
